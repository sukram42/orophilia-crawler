/*
This has been generated by the overpass-turbo wizard.
The original search was:
“tourism= alpine_huts”
*/
[out:json][timeout:25];
// gather results
(
  // SEARCH for ways (e.g parking spot or alpine hut) around a main node (e.g peak)
  // natural=peak
  node[{node_condition}](around:{node_radius}, {lat}, {lon});
  // tourism = alpine_hut
  way[{way_condition}](around:{way_radius}); 
);
// print results
out body center;
>;
out skel qt;